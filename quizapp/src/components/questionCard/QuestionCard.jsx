import React from 'react'
import './QuestionCard.css';

const QuestionCard = ({QuestionCard, score, setScore, count, setCount, modal, setModal}) => {
  return (
    <div>QuestionCard</div>
  )
}

export default QuestionCard